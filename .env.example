# Copy this file to ".env" (same folder as main.py) and set a real value.
# JWT_SECRET_KEY must be at least 32 characters long.
# The .env file is git-ignored, so your secret is never committed.
JWT_SECRET_KEY=change-me-to-a-long-random-secret-key-min-32-chars
